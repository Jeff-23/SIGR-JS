import { AlertTriangle, Inbox, LoaderCircle, RefreshCw, ServerOff, ShieldX } from "lucide-react";

export function LoadingState({ label = "Cargando información…" }: { label?: string }) { return <div className="empty" role="status"><LoaderCircle className="animate-spin" size={38}/><h2>{label}</h2></div>; }
export function EmptyState({ title, detail }: { title: string; detail: string }) { return <div className="empty"><Inbox size={38}/><h2>{title}</h2><p>{detail}</p></div>; }
export function ErrorState({ title = "No fue posible cargar la información", detail, requestId, retry }: { title?: string; detail?: string; requestId?: string; retry?: () => void }) { return <div className="empty" role="alert"><AlertTriangle size={38} className="text-orange-600"/><h2>{title}</h2>{detail && <p>{detail}</p>}{requestId && <code className="rounded-lg bg-denim/5 px-3 py-2 text-xs">Solicitud {requestId}</code>}{retry && <button onClick={retry} className="secondary mt-2 h-11 w-auto px-5"><RefreshCw size={17}/> Reintentar</button>}</div>; }
export function AccessDenied() { return <div className="empty min-h-[60vh]"><ShieldX size={48} className="text-orange-600"/><h2>Acceso restringido</h2><p>Tu rol, permisos o plan actual no habilitan esta sección.</p><a href="/" className="secondary mt-2 h-11 w-auto px-5">Volver al resumen</a></div>; }
export function ServiceUnavailable() { return <div className="empty min-h-[60vh]"><ServerOff size={48} className="text-orange-600"/><h2>Servicio temporalmente no disponible</h2><p>Puedes continuar con las funciones locales habilitadas. Reintentaremos cuando vuelva la conexión.</p><button onClick={() => window.location.reload()} className="secondary mt-2 h-11 w-auto px-5"><RefreshCw size={17}/> Reintentar</button></div>; }
